import 'package:flutter/material.dart';

class TodoNewItem extends StatelessWidget {
  const TodoNewItem({super.key});

  @override
  Widget build(Object context) {
    return const Text('Add new item');
  }
}
